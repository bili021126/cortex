/**
 * tui/renderer/index.ts — 渲染器 barrel 导出
 *
 * 仅保留活跃符号（D1 收敛：sanitize/token-monitor/tool-log/diff-viewer 已移除）。
 *
 * @module tui/renderer
 * @since v3 — Core-3 差分渲染
 */

export { SigintHandler } from "./sigint-handler.js";
