// @ci: unit
/**
 * @cortex/factory — smoke test
 */
import { describe, it, expect } from "vitest";

describe("@cortex/factory", () => {
  it("package loads without error", () => {
    expect(true).toBe(true);
  });
});
