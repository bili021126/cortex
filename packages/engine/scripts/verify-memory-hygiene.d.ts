/**
 * 记忆语义卫生五层验证脚本
 *
 * 用法: cd packages/engine && npx tsx scripts/verify-memory-hygiene.ts
 *
 * 验证内容:
 *   L1 SHA256 精确去重
 *   L2 BFS 噪声门限
 *   L3 权重自然老化
 *   L4 内存总量软上限
 *   L0 向量语义接入（需 @xenova/transformers 可用）
 */
export {};
//# sourceMappingURL=verify-memory-hygiene.d.ts.map