/**
 * amendment-judge 评判引擎单元测试。
 *
 * 覆盖：
 *   - 不可变原则阻断（触犯原则一）
 *   - 正确提案全通过
 *   - 虚假 before 段落阻断
 *   - 版本号回退阻断
 *   - 缺失交叉引用
 *   - 摘要/理由过短 NEEDS_CLARIFICATION
 *   - breaking=true → APPROVED_WITH_CAVEATS
 */
export {};
//# sourceMappingURL=amendment-judge.test.d.ts.map