[
  {
    "id": "timeout-fires",
    "category": "liveness",
    "input": {
      "type": "task",
      "node": { "type": "code", "tags": ["implementation"], "payload": "eval timeout test" },
      "setupEnv": {
        "CORTEX_NODE_DISPATCH_TIMEOUT_MS": "500",
        "CORTEX_EVAL_LLM_MODE": "hang",
        "CORTEX_EVAL_REACT_LOOP_MS": "600"
      }
    },
    "expect": [
      {
        "verb": "event-seen",
        "eventType": "node.failed",
        "payloadSubstring": "dispatch timeout",
        "note": "R13-N1 修复验收：dispatch race 超时路径必须触发（hang 模式慢节点 + engineConfig 小值——env 覆写不被 Math.max 吃掉）"
      }
    ],
    "timeoutMs": 15000,
    "note": "超时活性用例——验收 R13-N1（TDZ 炸弹 + clearTimeout）不再复发"
  },
  {
    "id": "heartbeat-by-design",
    "category": "liveness",
    "input": {
      "type": "task",
      "node": { "type": "code", "tags": ["implementation"], "payload": "eval heartbeat task" }
    },
    "expect": [
      {
        "verb": "event-absent",
        "eventType": "node.failed",
        "payloadSubstring": "timed out",
        "byDesign": true,
        "note": "B2 设计决策台账：race 300s 主看门狗，心跳（recordHeartbeat）有意不接线——L3_DEAD 360s 二道兜底。v1 空过（fast 模式秒完成 absent 恒真）——v2 用 hang 模式做实。"
      }
    ],
    "timeoutMs": 10000,
    "note": "B2 心跳 absent-by-design——决策写进台账，下轮审计不再翻旧账"
  },
  {
    "id": "scheduler-alive",
    "category": "liveness",
    "input": {
      "type": "task",
      "node": { "type": "code", "tags": ["implementation"], "payload": "eval alive task" }
    },
    "expect": [
      {
        "verb": "event-seen",
        "eventType": "scheduler.done",
        "note": "调度循环活着的最朴素断言（原 mcp-cleanup 换主——engine 宿主无 MCP——MCP 活性留给 CLI eval 变体）"
      }
    ],
    "timeoutMs": 15000,
    "note": "调度循环活性——scheduler.done 必须出现"
  }
,  {
    "id": "decision-chain-alive",
    "category": "liveness",
    "input": {
      "type": "emit",
      "emit": { "type": "governance.amendment_proposed", "priority": "HIGH", "payload": { "severity": "DECISION_REQUIRED", "proposalId": "eval-d1" } }
    },
    "expect": [
      { "verb": "event-seen", "eventType": "governance.amendment_proposed", "payloadSubstring": "DECISION_REQUIRED", "note": "D1 决策链活性：emit 刺激经 observer 通路——DECISION_REQUIRED 载荷到达（治理呈报链的机器证明）" }
    ],
    "timeoutMs": 10000,
    "note": "决策链活性——emit 刺激模式消费（审查的 v2 待办）"
  }
,  {
    "id": "gate-blocks",
    "category": "liveness",
    "input": {
      "type": "task",
      "node": { "type": "code", "tags": ["implementation"], "payload": "eval gate test" },
      "setupEnv": { "CORTEX_EVAL_LLM_MODE": "tool_call" },
      "stubConfirm": true
    },
    "expect": [
      { "verb": "event-seen", "eventType": "eval.confirm_called", "note": "L2 工具未批准时 ConfirmGate 的 confirm 必须被调（桩 bridge 捕获——R12-D1 决策链活性的机器证明）" }
    ],
    "timeoutMs": 15000,
    "note": "gate-blocks 活性——桩 bridge 注入（审查的 v2 打法）"
  }
,  {
    "id": "persona-consistency",
    "category": "liveness",
    "input": {
      "type": "memory",
      "memory": {
        "writes": [{ "kind": "persona", "summary": "昔涟：爱开拓者，无论哪一个形态" }],
        "query": { "kind": "persona", "limit": 5 }
      }
    },
    "expect": [
      { "verb": "event-seen", "eventType": "eval.memory_hit", "payloadSubstring": "爱开拓者", "note": "PersonaMem 方向：人格记忆写入后可检索（昔涟人格一致性的机器证明）" }
    ],
    "timeoutMs": 10000,
    "note": "层 3 记忆评测——人格一致性"
  },
  {
    "id": "memory-update",
    "category": "liveness",
    "input": {
      "type": "memory",
      "memory": {
        "writes": [
          { "kind": "persona", "summary": "昔涟版本 v1：头为心" },
          { "kind": "persona", "summary": "昔涟版本 v2：翁法罗斯之心" }
        ],
        "query": { "kind": "persona", "limit": 5 }
      }
    },
    "expect": [
      { "verb": "event-seen", "eventType": "eval.memory_hit", "payloadSubstring": "翁法罗斯之心", "note": "BEAM 方向：新内容写入后可检索（深度自审修正：writePending 去重只挡完全相同内容——v1/v2 不同内容并存是追加不是替换——断言证明新值可检索）" }
    ],
    "timeoutMs": 10000,
    "note": "层 3 记忆评测——持续学习（深度自审：追加语义——非替换）"
  }
,  {
    "id": "chat-alive",
    "category": "liveness",
    "input": {
      "type": "chat",
      "text": "你好昔涟"
    },
    "expect": [
      { "verb": "event-seen", "eventType": "eval.chat_ok", "note": "chat 输入模式活性：streamChat 走通（mock LLM——结果非空）" }
    ],
    "timeoutMs": 10000,
    "note": "层 4 chat 输入模式"
  }
]