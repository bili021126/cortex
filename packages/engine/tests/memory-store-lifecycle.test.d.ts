/**
 * 测试文件: MemoryStore 生命周期状态机测试
 *
 * @since v3.0.0 — 适配器委托 @cortex/memory 后端，简化生命周期验证。
 *
 * 测试范围:
 * - init() → 进入就绪状态
 * - write() → read() 正常读写
 * - close() → 拒绝新写入/读取
 * - close() 幂等
 */
export {};
//# sourceMappingURL=memory-store-lifecycle.test.d.ts.map