/**
 * 测试文件: ShutdownWarden 优雅关闭监护
 *
 * @since v3.1.0
 *
 * 测试范围:
 * - shutdown() 完整序列 (LifecycleManager → endSession → close)
 * - 组件失败时的容错
 * - ShutdownReport 字段验证
 * - 无 memory 场景
 */
export {};
//# sourceMappingURL=shutdown-warden.test.d.ts.map