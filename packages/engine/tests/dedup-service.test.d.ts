/**
 * 测试文件: DedupService 内容去重服务
 *
 * @since v3.1.0
 *
 * 测试范围:
 * - contentHash() — SHA256 确定性
 * - exactMatch() — content_hash 精确匹配
 * - vectorDedup() — 余弦相似度去重
 * - 边界: 空向量, 零向量, 不同维度, 空池
 */
export {};
//# sourceMappingURL=dedup-service.test.d.ts.map