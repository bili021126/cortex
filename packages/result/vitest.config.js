import { defineConfig } from "vitest/config";
/**
 * @cortex/result vitest 配置。
 * 从项目根目录运行：npx vitest run --root result
 */
export default defineConfig({
    test: {
        include: ["tests/**/*.test.ts"],
    },
});
//# sourceMappingURL=vitest.config.js.map