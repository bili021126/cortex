/**
 * @ci: unit
 * tool-call-stream.test.ts — accumulateToolCalls / finalizeToolCalls 单元测试
 *
 * 重点验证：后续 delta 不带 id 只有 index 时，arguments 正确追加到同一槽位。
 */
export {};
//# sourceMappingURL=tool-call-stream.test.d.ts.map