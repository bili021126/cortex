/**
 * @cortex/notification — smoke test
 */
import { describe, it, expect } from "vitest";

describe("@cortex/notification", () => {
  it("package loads without error", () => {
    expect(true).toBe(true);
  });
});
