/**
 * @cortex/config — 技能系统常量
 *
 * @module constants/skills
 * @layer root
 */

/** 可执行技能默认超时 (ms) */
export const DEFAULT_SKILL_TIMEOUT_MS = 30_000;

/** 可执行技能默认最大重试次数 */
export const DEFAULT_SKILL_MAX_RETRIES = 0;
