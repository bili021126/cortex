/**
 * @cortex/config — 超时常量
 *
 * @module constants/timeouts
 * @layer root
 */
/** 工具调用最大轮次（TUI 对话模式） */
export declare const DEFAULT_MAX_TOOL_ROUNDS = 20;
/** 任务执行默认超时（秒） */
export declare const DEFAULT_TASK_TIMEOUT_SEC = 300;
/** 命令分发超时（秒） */
export declare const DEFAULT_COMMAND_TIMEOUT_SEC = 60;
/** 输出格式默认值 */
export declare const DEFAULT_OUTPUT_FORMAT: "text";
//# sourceMappingURL=timeouts.d.ts.map