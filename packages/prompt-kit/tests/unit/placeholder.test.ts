// @ci: unit
/**
 * @cortex/prompt-kit — 占位测试
 *
 * 占位测试确保测试基础设施正常。
 * Phase 1 实施时将替换为真实测试。
 */

import { describe, it, expect } from "vitest";

describe("@cortex/prompt-kit", () => {
  it("测试基础设施正常运行", () => {
    expect(1 + 1).toBe(2);
  });
});
