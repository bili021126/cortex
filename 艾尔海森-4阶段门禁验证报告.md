# 艾尔海森 · 4 阶段门禁验证报告

> 数据来源：`scripts/ci-gate.ts` + `skills/skill-p10-ci-gate-full-cycle.json`  
> 验证目标：确认"4 阶段门禁"的划分存在性、阻断顺序合理性

---

## 一、4 阶段门禁定义源

**真相源文件**：`scripts/ci-gate.ts`（第 61 行）

```typescript
// ── 门禁栈：类型检查 → 修复验证 → 契约验证 → 单元测试 ──
```

输出标注：

```
🔒 [门禁 1/4] tsc --noEmit 全量类型检查...
...
🧪 vitest 按包串行 — unit + verify + contract (...个文件)
```

---

## 二、4 阶段明细

| 阶段 | 标识 | 阻断语义 | 实现方式 | 阻断条件 |
|------|------|---------|---------|---------|
| **1/4** 全量类型检查 | `tsc --noEmit` | 硬阻断 | `npx tsc --noEmit -p tsconfig.json` | 任何类型错误 → `process.exit(1)` |
| **2/4** 修复验证 | `@ci: verify` | 软阻断（同一 vitest 流程） | 按包串行 vitest，`--exclude` 排除非目标标签 | 测试失败 → `allOk = false` |
| **3/4** 契约验证 | `@ci: contract` | 软阻断（同一 vitest 流程） | 按包串行 vitest，`--exclude` 排除非目标标签 | 测试失败 → `allOk = false` |
| **4/4** 单元测试 | `@ci: unit` | 软阻断（同一 vitest 流程） | 按包串行 vitest，默认标签，fallback | 测试失败 → `allOk = false` |

**关键代码证据**（ci-gate.ts L61-L77）：

```typescript
// ── 门禁栈：类型检查 → 修复验证 → 契约验证 → 单元测试 ──
if (!dryRun) {
    console.log("\n🔒 [门禁 1/4] tsc --noEmit 全量类型检查...");
    // tsc 失败 → process.exit(1)，后续不执行
}
// 后续合并执行：@ci: unit + verify + contract
const targetFiles = all.filter((t) => t.ciTag === "unit" || t.ciTag === "verify" || t.ciTag === "contract");
```

---

## 三、阻断顺序合理性分析

### 3.1 第 1 阶段：tsc 类型检查 → 第 2-4 阶段

**合理性：✅ 合理**

理由：
- 类型错误是编译期错误，比运行时错误更基础、更早被发现
- 类型错误不通过 → 测试代码可能根本不可达（import/resolve 失败）
- ci-gate.ts 明确用 `process.exit(1)` 硬阻断，第 2-4 阶段完全不执行

> **风险控制逻辑**：类型系统是防线的第一层——类型错了一切免谈。

### 3.2 第 2 阶段：verify → 第 3 阶段：contract → 第 4 阶段：unit

**合理性：⚠️ 阶段间阻断语义缺失，但设计意图合理**

现状问题：
- 第 2-4 阶段**在同一 vitest 流程中并发执行**，没有显式的阶段间阻断
- 如果 verify 测试失败，contract 和 unit 仍会继续跑（所有测试搜集后统一判断 `allOk`）

设计意图分析：
- verify（修复验证）比 contract（跨包契约）更基础 — **合理**：修复验证是局部正确性，契约验证是全局接口兼容性
- contract（跨包契约）比 unit（单元测试）更基础 — **合理**：接口契约破裂意味着单元测试的测试环境本身不可信
- 但目前实现层面**未在第一阶段失败后阻断第二阶段执行**，这是 gap

### 3.3 @ci 标签分层

| 标签 | 层次 | CI 执行 | 阻断优先级 |
|------|------|---------|-----------|
| `unit` | 单元测试 — 最细粒度功能验证 | 必跑 | 低（底层） |
| `verify` | 关键修复验证 — 局部正确性 | 必跑 | 中 |
| `contract` | 跨包接口契约 — 全局兼容性 | 必跑 | 高（顶层） |
| `llm` | 需要 LLM API | 跳过 | — |
| `integration` | 需要外部服务 | 跳过 | — |
| `e2e` | 端到端测试 | 跳过 | — |
| `manual` | 人工触发 | 永远跳过 | — |

---

## 四、与 skill-p10 的对比

| 维度 | ci-gate.ts（4 阶段门禁） | skill-p10-ci-gate-full-cycle.json |
|------|------------------------|----------------------------------|
| 阶段数 | 4 阶段 | 4 步 |
| 阶段 | tsc → verify → contract → unit | build → typecheck → test → lint |
| 阻断 | tsc 硬阻断，其余软阻断 | 显式中止后续步骤 |
| 粒度 | 标签级（@ci）选择性执行 | 全量流程 |
| 场景 | 日常 CI 门禁 | 完整部署门禁 |

**两者互补，无冲突**：ci-gate.ts 是日常增量验证（仅跑 unit/verify/contract），skill-p10 是全量构建验证（build → typecheck → test → lint）。

---

## 五、验证结论

| 验证项 | 结果 | 证据 |
|--------|------|------|
| 4 阶段划分是否存在 | ✅ 存在 | `门禁栈：类型检查 → 修复验证 → 契约验证 → 单元测试`（ci-gate.ts L61） |
| 阶段间阻断顺序是否合理 | ✅ 第 1→2 合理；⚠️ 2→3→4 无阶段间硬阻断 | tsc 硬阻断 exit(1)；verify/contract/unit 在同一 vitest 流程并发 |
| 各阶段职责是否可区分 | ✅ 是 | @ci 标签机制（unit/verify/contract）明确定义语义分层 |
| 阻断语义是否满足"越基础越靠前" | ✅ 是 | 类型系统（最基础）→ 修复验证（局部）→ 契约（全局）→ 单元（最细粒度兜底） |
| 配置源是否明确 | ✅ 是 | 单文件 `scripts/ci-gate.ts` 为真相源，无散落配置 |

### 建议优化项（非阻断）

1. **第 2-4 阶段间增加阶段阻断**：若 verify 全部失败，应跳过 contract 和 unit 执行，避免浪费计算资源
2. **@ci 标签缺失审计暂不断路**：当前 `untagged` 仅 warn 不 error，建议设定过渡期后升格为 error
3. **verify/contract 测试文件数量可见性**：当前仅统计 total/unit/verify/contract 文件数，建议按包展示 verify/contract 覆盖率

---

**艾尔海森 · 数据验证签名**：4 阶段门禁结构合理，配置源唯一，阻断顺序满足"越基础越靠前"原则。第 2-4 阶段间缺乏硬阻断为已知 gap，建议但不阻断。

报告时间：${new Date().toISOString()}
